/**
 * Module dependencies.
 */
import OAuth2Strategy from "./oauth2";

/**
 * Framework version.
 */
require("pkginfo")(module, "version");

/**
 * Expose constructors.
 */

export { OAuth2Strategy as Strategy };
